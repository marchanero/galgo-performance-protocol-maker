\documentclass[runningheads]{llncs}
%
\usepackage[T1]{fontenc}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{threeparttable}
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.multipart, fit, backgrounds, calc, chains}

% ColorBrewer-inspired colorblind-safe palette for figures
\definecolor{blue1}{HTML}{3182BD}
\definecolor{blue2}{HTML}{DEEBF7}
\definecolor{green1}{HTML}{31A354}
\definecolor{green2}{HTML}{E5F5E0}
\definecolor{orange1}{HTML}{E6550D}
\definecolor{orange2}{HTML}{FEE6CE}
\definecolor{purple1}{HTML}{756BB1}
\definecolor{purple2}{HTML}{E7E1EF}
\definecolor{red1}{HTML}{D73027}
\definecolor{red2}{HTML}{FDDBC7}
\definecolor{gray1}{HTML}{636363}
\definecolor{gray2}{HTML}{F0F0F0}
\definecolor{gray3}{HTML}{BDBDBD}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage{hyperref}
%
\begin{document}
%
\title{Efficient Transformer Architectures for\\Electrodermal Activity-based Arousal Classification}
%
\titlerunning{Efficient Transformers for EDA-based Arousal Classification}
%
\author{
Roberto S\'{a}nchez-Reolid\inst{1,2}\orcidID{0000-0003-4455-370X} \and
Francisco Javier Celdr\'{a}n\inst{1}\orcidID{0009-0000-2699-219X} \and
Daniel S\'{a}nchez-Reolid\inst{3}\orcidID{0000-0003-3612-1261} \and
Antonio Fern\'{a}ndez-Caballero\inst{1,3,4}\orcidID{0000-0002-8211-0398}
}
\authorrunning{R. S\'{a}nchez-Reolid et al.}
%
\institute{
Instituto de Investigaci\'{o}n en Inform\'{a}tica de Albacete (I3A), \\Universidad de Castilla-La Mancha, Albacete, Spain
\and
Departamento de Tecnolog\'{i}as y Sistemas de Informaci\'{o}n, \\Universidad de Castilla-La Mancha, Talavera de la Reina, Spain\\
\email{roberto.sanchez@uclm.es}
\and
Departamento de Sistemas Inform\'{a}ticos, Universidad de Castilla-La Mancha, Albacete, Spain
\and
Biomedical Research Networking Centre in Mental Health, Madrid, Spain
}
%
\maketitle
%
\begin{abstract}
Electrodermal Activity (EDA) is a reliable, non-invasive biomarker of sympathetic arousal increasingly used in wearable affective computing. While recent work has demonstrated that Transformer-based architectures outperform convolutional baselines for subject-independent arousal classification from EDA, their computational cost limits practical deployment in resource-constrained wearable and mobile devices. This work presents the first systematic comparison of efficient and modern architectures spanning five paradigms---patch-based attention (PatchTST), sparse attention (Informer), frequency-domain modelling (Autoformer, FEDformer, TimesNet), state space models (Mamba), and modernised convolutions (ModernTCN)\/linear models (DLinear)---for binary arousal classification from EDA signals collected from 147 participants under a controlled laboratory protocol. All models are evaluated using strict Leave-One-Subject-Out (LOSO) cross-validation to ensure subject-independent generalisation. Beyond predictive accuracy, we introduce computational efficiency (parameter count, FLOPs, inference time, and memory footprint) as a primary evaluation dimension, enabling explicit characterisation of the accuracy-efficiency Pareto frontier across architectures. By jointly evaluating classification performance and resource consumption, this work provides actionable guidance for selecting and deploying Transformer-based, SSM-based, and convolutional arousal classifiers on edge devices.
\keywords{Electrodermal Activity \and Arousal Classification \and Efficient Transformers \and Lightweight Deep Learning \and Subject-Independent Evaluation \and Edge Deployment}
\end{abstract}
%
% ======================================================
\section{Introduction}

Electrodermal Activity (EDA) reflects sympathetic nervous system activation through sudomotor nerve activity, providing a robust and non-concealable biomarker for emotional arousal \cite{Hossain2024,Meijer2023}. Unlike other peripheral physiological signals such as heart rate or respiration, EDA is exclusively innervated by the sympathetic branch of the autonomic nervous system, making it a uniquely specific indicator of arousal without parasympathetic confounds \cite{LongTermVariability}. The EDA signal comprises two superimposed components: a slow-varying tonic level reflecting prolonged arousal states, and rapid phasic skin conductance responses (SCRs) that reflect immediate, stimulus-evoked sympathetic activation \cite{sanchez2020deep}.

The growing availability of wrist-worn and palm-based wearable sensors capable of continuous EDA monitoring \cite{Schmidt2018WESAD} has generated substantial interest in deploying machine learning models for automatic arousal classification directly on resource-constrained edge devices. Applications span mental health monitoring, stress detection in occupational settings, adaptive human-computer interaction, and real-time biofeedback for anxiety management \cite{Mukhopadhyay2024,Naithani2026}. However, translating these laboratory-validated models to practical wearable deployment requires architectures that simultaneously achieve high classification accuracy under subject-independent conditions, low computational cost, and minimal memory footprint---a combination that has received limited systematic investigation.

In prior work \cite{SanchezReolid2022}, we conducted a systematic comparison of five deep learning architectures---1D-CNN, Temporal Convolutional Networks (TCN), InceptionTime, Time-Series Transformer (TST), and PatchTST---for subject-independent binary arousal classification from EDA. Under strict Leave-One-Subject-Out (LOSO) validation with 147 participants, Transformer-based architectures consistently outperformed convolutional baselines. PatchTST achieved the best performance (F1 = 0.852, AUC = 0.912), demonstrating that global dependency modelling through self-attention captures physiologically relevant temporal patterns more effectively than fixed convolutional receptive fields. Interpretability analyses further revealed that Transformer attention mechanisms concentrate on SCR onset and rising phases, aligning with established physiological knowledge of sympathetic activation dynamics.

Despite these findings, PatchTST incurs non-trivial computational demands. Although patch-based tokenisation reduces the effective sequence length from $T$ to $N \ll T$, the core self-attention operation retains $O(N^2)$ complexity and the architecture requires multiple encoder layers with learned positional embeddings, feed-forward projections, and layer normalisation. For a 40-second EDA window sampled at 4 Hz ($T = 160$), this translates to a forward pass whose latency and memory consumption may exceed the constraints of low-power microcontrollers and mobile processors typical of wearable devices. This practical limitation motivates a focused investigation of more aggressively efficient architectures.

The time-series forecasting community has developed a rich family of efficient Transformer variants that achieve sub-quadratic or even linear complexity through fundamentally different attention mechanisms \cite{Wen2023}. The Informer \cite{Informer2021} introduced ProbSparse self-attention, which measures query sparsity through the Kullback-Leibler divergence between the attention probability distribution and a uniform distribution, reducing time and memory complexity from $O(L^2)$ to $O(L \log L)$. The Autoformer \cite{Autoformer2021} replaced point-wise attention entirely with an Auto-Correlation mechanism that computes series-wise correlations in the frequency domain based on discovered periodicities, while integrating progressive seasonal-trend decomposition as an internal architectural block. The FEDformer \cite{FEDformer2022} achieved $O(L)$ complexity---the only linear-complexity candidate considered---by operating in the frequency domain through Fourier-enhanced attention blocks that randomly select a subset of Fourier modes. Concurrently, the provocative DLinear model \cite{DLinear2023} demonstrated that a decomposition into trend (moving average) and seasonal (residual) components followed by independent one-layer linear networks can outperform sophisticated Transformer architectures on multiple forecasting benchmarks, raising fundamental questions about the necessity of attention mechanisms for temporal modelling.

Despite these advances in the forecasting literature, no systematic evaluation of efficient Transformer architectures exists for physiological signal classification under rigorous subject-independent validation protocols. This gap is significant for two reasons. First, physiological signals such as EDA exhibit distinct statistical properties---including inter-subject variability, non-stationarity, and structured phasic responses---that may interact differently with each efficiency mechanism compared to the electricity, traffic, and weather datasets on which these architectures were originally benchmarked. Second, the forecasting task differs fundamentally from classification: the former predicts continuous future values, while the latter aggregates temporal information into a discrete decision boundary, potentially altering the relative importance of different architectural components such as the decoder and the aggregation mechanism \cite{TST_Zerveas2021}. Bridging this gap requires empirical evaluation under a task and data distribution that reflect the intended deployment scenario.

This work addresses this gap by presenting the first systematic comparison of efficient and modern architectures for EDA-based arousal classification. Eight architectures spanning five distinct efficiency paradigms are evaluated: PatchTST (patch-based tokenisation, serving as our prior baseline), Informer (ProbSparse attention, $O(L \log L)$), Autoformer (Auto-Correlation with progressive decomposition, $O(L \log L)$), TimesNet (FFT-based 1D$\to$2D transformation, $O(L \log L)$), FEDformer (frequency-domain attention, $O(L)$), Mamba (selective state space model, $O(L)$), ModernTCN (modernised depthwise convolutions, $O(L)$), and DLinear (simple linear decomposition, $O(L)$). All architectures are assessed on the identical dataset of 147 participants and LOSO protocol as our prior work \cite{SanchezReolid2022}, ensuring direct comparability of results. Crucially, we elevate computational efficiency---measured through parameter count, FLOPs, inference time, and memory footprint---to a primary evaluation dimension alongside standard classification metrics.

The contributions of this work are fourfold: (i) the first systematic comparison of efficient and modern architectures---spanning Transformers, state space models, and modernised convolutions---for subject-independent EDA-based arousal classification under strict LOSO validation; (ii) explicit characterisation of the accuracy-efficiency Pareto frontier through joint evaluation of predictive performance and resource consumption, directly informing architectural selection for edge deployment; (iii) inclusion of DLinear and ModernTCN as critical ``sanity check'' baselines, testing whether complex attention or state-space mechanisms provide measurable benefit over simple linear decomposition and modernised convolutions for EDA signals; and (iv) analysis of how each efficiency mechanism interacts with the specific temporal dynamics of phasic EDA responses, contributing physiologically-grounded insights into architecture design for biosignal classification.

% ======================================================
\section{Method}

\subsection{Dataset and Experimental Protocol}

The experimental dataset follows a controlled stress elicitation protocol described in previous work \cite{sanchez2020deep}. Electrodermal Activity signals were recorded from 147 healthy participants (aged 18--44 years) exposed to audiovisual stimuli designed to induce calm and stress states under controlled laboratory conditions. Each stimulus lasted 47 seconds. To eliminate transition artefacts associated with stimulus onset and offset, the first 4 seconds and the last 3 seconds were removed, resulting in 40-second effective segments per trial. Signals were acquired at a constant sampling frequency of 4 Hz.

Each participant completed multiple trials in both calm and stress conditions, producing a balanced binary classification dataset. All trials belonging to the same participant were preserved within the same fold during cross-validation to prevent subject-level data leakage---a critical methodological safeguard given the strong inter-subject variability characteristic of EDA signals \cite{LongTermVariability,Azad2025}.

\subsection{EDA Signal Processing and Input Representation}

Raw skin conductance signals were preprocessed following the pipeline established in our prior work \cite{SanchezReolid2022}. A low-pass FIR filter (cut-off frequency: 4 Hz) was applied, followed by Gaussian smoothing to attenuate acquisition noise. Continuous Decomposition Analysis (CDA) was used to separate the tonic skin conductance level ($SCL$) and the phasic skin conductance response ($SCR$):

\begin{equation}
SC(t) = SCL(t) + SCR(t)
\end{equation}

Only the phasic component $SCR(t)$ was retained for modelling, as it directly reflects sympathetic nervous system activation uncontaminated by slow tonic drift. To enrich the short-term dynamic representation and facilitate early detection of sympathetic responses, two derivative channels were computed from the phasic signal: the first temporal derivative ($\Delta SCR$) and the second temporal derivative ($\Delta^2 SCR$). These channels encode the velocity and acceleration of sympathetic activation respectively, providing the model with explicit information about the rate and pattern of phasic change \cite{sanchez2022one}.

Each temporal window of length $n$ seconds was thus represented as:

\begin{equation}
\mathbf{X} \in \mathbb{R}^{T \times C}, \quad T = 4n,\; C = 3
\end{equation}

\noindent where $C = 3$ corresponds to the channels $(SCR, \Delta SCR, \Delta^2 SCR)$.

To analyse the minimal temporal interval required for reliable arousal classification, window lengths were systematically varied from $n = 1$ to $n = 40$ seconds. Two segmentation strategies were evaluated: (i) non-overlapping windows and (ii) sliding windows with 1-second stride (4 samples). As noted above, all windows belonging to the same subject were preserved within the same LOSO fold.

\begin{figure}[t]
\centering
\resizebox{\linewidth}{!}{%
\begin{tikzpicture}[
    node distance=0.5cm and 0.35cm,
    % ColorBrewer palette
    proc/.style={draw=gray1, fill=gray2, rounded corners=2pt, minimum width=1.5cm, minimum height=0.6cm, align=center, font=\footnotesize, inner sep=4pt},
    proc_blue/.style={proc, fill=blue2, draw=blue1},
    proc_green/.style={proc, fill=green2, draw=green1},
    proc_orange/.style={proc, fill=orange2, draw=orange1},
    proc_red/.style={proc, fill=red2, draw=red1},
    proc_purple/.style={proc, fill=purple2, draw=purple1},
    group/.style={draw=gray3, dashed, rounded corners=3pt, inner xsep=6pt, inner ysep=5pt},
    arr/.style={-{Stealth[scale=0.6]}, thick, draw=gray1},
    arr_gray/.style={arr, draw=gray3},
    lab/.style={font=\footnotesize, text=gray1},
]
% === Row 0: Preprocessing pipeline ===
\node[proc_blue] (raw) {Raw EDA\\$SC(t)$};
\node[proc_blue, right=of raw] (filt) {Low-pass FIR\\(4 Hz)};
\node[proc_blue, right=of filt] (cda) {CDA\\Decomp.};
\node[proc_blue, right=of cda] (deriv) {Derivative\\Channels};
\node[proc_blue, right=of deriv] (window) {Window\\Segment};
\node[proc_blue, right=of window] (norm) {Z-score\\(per fold)};
\draw[arr] (raw) -- (filt);
\draw[arr] (filt) -- (cda);
\draw[arr] (cda) -- (deriv);
\draw[arr] (deriv) -- (window);
\draw[arr] (window) -- (norm);
\node[lab, above=0.1cm of raw] {\textbf{Preprocessing}};
\node[lab, above=0.1cm of norm, text=blue1] {$\mathbf{X} \in \mathbb{R}^{T \times 3}$};

% === Row 1: Attention O(N²) ===
\node[proc_green, below=1.0cm of raw, minimum width=1.4cm] (p1) {PatchTST};
% === Row 1: O(L log L) ===
\node[proc_green, right=0.2cm of p1, minimum width=1.4cm] (i1) {Informer};
\node[proc_green, right=0.2cm of i1, minimum width=1.4cm] (a1) {Autoformer};
\node[proc_green, right=0.2cm of a1, minimum width=1.4cm] (t1) {TimesNet};
% === Row 1: O(L) ===
\node[proc_orange, right=0.2cm of t1, minimum width=1.4cm] (f1) {FEDformer};
\node[proc_orange, right=0.2cm of f1, minimum width=1.4cm] (m1) {Mamba};
\node[proc_orange, right=0.2cm of m1, minimum width=1.4cm] (c1) {ModernTCN};
% === Row 1: Linear baseline ===
\node[proc_red, right=0.2cm of c1, minimum width=1.4cm] (d1) {DLinear};

% Arrows from preprocessing to all models
\draw[arr_gray] (norm.south) -- ++(0,-0.2) -| (p1.north);
\draw[arr_gray] (norm.south) -- ++(0,-0.2) -| (i1.north);
\draw[arr_gray] (norm.south) -- ++(0,-0.2) -| (a1.north);
\draw[arr_gray] (norm.south) -- ++(0,-0.2) -| (t1.north);
\draw[arr_gray] (norm.south) -- ++(0,-0.2) -| (f1.north);
\draw[arr_gray] (norm.south) -- ++(0,-0.2) -| (m1.north);
\draw[arr_gray] (norm.south) -- ++(0,-0.2) -| (c1.north);
\draw[arr_gray] (norm.south) -- ++(0,-0.2) -| (d1.north);

% Complexity group boxes
\begin{scope}[on background layer]
\node[group, fit=(p1), label={[font=\footnotesize\bfseries]north:{$O(N^2)$}}] {};
\node[group, fit=(i1)(a1)(t1), label={[font=\footnotesize\bfseries]north:{$O(L \log L)$}}] {};
\node[group, fit=(f1)(m1)(c1), label={[font=\footnotesize\bfseries]north:{$O(L)$}}] {};
\node[group, fit=(d1), label={[font=\footnotesize\bfseries]north:{$O(L)$}}] {};
\end{scope}

% === Row 2: Classification + Metrics ===
\node[proc_purple, below=1.6cm of a1, minimum width=3.5cm] (class) {Binary Classification\\Calm / Stress};
\node[proc_purple, right=1.2cm of class, minimum width=3.5cm] (perf) {Performance Metrics\\Acc, Prec, Rec, F1, AUC};
\draw[arr] (class) -- (perf);

% Model → classifier arrows
\foreach \m in {p1,i1,a1,t1,f1,m1,c1,d1} {
    \draw[arr_gray] (\m.south) -- ++(0,-0.15) -| (class.north);
}

% === Row 3: Efficiency ===
\node[proc, fill=cyan!10, draw=cyan!60!black, below=0.7cm of perf, minimum width=9cm, font=\footnotesize] (eff) {Efficiency Metrics: $N_{\text{params}}$ (M) \quad FLOPs (M) \quad $t_{\text{inf}}$ (ms) \quad $M_{\text{peak}}$ (MB)};
\draw[arr_gray] (perf.south) -- ++(0,-0.2) -| (eff.north);
\end{tikzpicture}%
}
\caption{Overall experimental pipeline. \textbf{Top:} EDA signal preprocessing and input construction. \textbf{Middle:} eight architectures grouped by computational complexity class. \textbf{Bottom:} binary classification output and joint evaluation of predictive performance and resource consumption. Color coding: blue = preprocessing, green = $O(L \log L)$, orange = $O(L)$, red = linear baseline, purple = evaluation.}
\label{fig:pipeline}
\end{figure}

\subsection{Efficient and Modern Architectures}

Eight architectures were selected to represent five distinct efficiency paradigms in time-series modelling (Table~\ref{tab:architectures}). For all Transformer-based architectures, the original forecasting head was replaced with a global average pooling operation over the encoded sequence followed by a linear classification layer. DLinear, which does not employ an encoder-decoder structure, was adapted by feeding the decomposed representations into a linear classification head.

\begin{table}[!ht]
\centering
\caption{Overview of evaluated architectures and their efficiency mechanisms. $L$ denotes input sequence length, $N$ denotes the number of patches after tokenisation ($N \ll L$), and $M$ denotes the number of selected Fourier modes ($M \ll L$).}
\label{tab:architectures}
\begin{tabular}{lccc}
\toprule
Architecture & Core Mechanism & Complexity & Venue \\
\midrule
PatchTST   & Patch tokenisation + self-attention & $O(N^2)$ & ICLR 2023 \\
Informer   & ProbSparse self-attention & $O(L \log L)$ & AAAI 2021 \\
Autoformer & Auto-Correlation + decomposition & $O(L \log L)$ & NeurIPS 2021 \\
TimesNet   & FFT-based 1D$\to$2D + Inception blocks & $O(L \log L)$ & ICLR 2023 \\
FEDformer  & Fourier-enhanced attention & $O(L)$ & ICML 2022 \\
Mamba      & Selective state space model (BiSSM) & $O(L)$ & arXiv 2023 \\
ModernTCN  & Modernised depthwise convolutions & $O(L)$ & ICLR 2024 \\
DLinear    & Trend-seasonal linear decomposition & $O(L)$ & AAAI 2023 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[p]
\centering
\resizebox{\linewidth}{!}{%
\begin{tikzpicture}[
    block/.style={draw=gray1, rounded corners=3pt, minimum width=5.2cm, minimum height=0.5cm, align=center, font=\footnotesize, inner sep=3pt},
    arr/.style={-{Stealth[scale=0.5]}, draw=gray3, thick},
    group/.style={draw=gray3, dashed, rounded corners=5pt, inner xsep=10pt, inner ysep=10pt},
    glabel/.style={font=\footnotesize\bfseries, label distance=8pt},
]
% ===== LEFT COLUMN =====
% --- PatchTST (blue) ---
\node[block, fill=blue2, draw=blue1] (p1) at (0,0) {Patch tokenisation};
\node[block, fill=blue2, draw=blue1, below=0.35cm of p1] (p2) {Self-attention};
\node[block, fill=blue2, draw=blue1, below=0.35cm of p2] (p3) {Global pooling $\to$ Linear classifier};
\draw[arr] (p1.south) -- (p2.north);
\draw[arr] (p2.south) -- (p3.north);
\begin{scope}[on background layer]
\node[group, draw=blue1, fit=(p1)(p2)(p3), label={[glabel, text=blue1]north:{PatchTST \hfill $O(N^2)$ \hfill \textit{ICLR 2023}}}] {};
\end{scope}

% --- Informer (green) ---
\node[block, fill=green2, draw=green1, below=1.6cm of p3] (i1) {ProbSparse self-attention};
\node[block, fill=green2, draw=green1, below=0.35cm of i1] (i2) {Self-attention distilling};
\node[block, fill=green2, draw=green1, below=0.35cm of i2] (i3) {Global pooling $\to$ Linear classifier};
\draw[arr] (i1.south) -- (i2.north);
\draw[arr] (i2.south) -- (i3.north);
\begin{scope}[on background layer]
\node[group, draw=green1, fit=(i1)(i2)(i3), label={[glabel, text=green1]north:{Informer \hfill $O(L \log L)$ \hfill \textit{AAAI 2021}}}] {};
\end{scope}

% --- Autoformer (green) ---
\node[block, fill=green2, draw=green1, below=1.6cm of i3] (a1) {Auto-Correlation (FFT-based)};
\node[block, fill=green2, draw=green1, below=0.35cm of a1] (a2) {Progressive trend-season decomp.};
\node[block, fill=green2, draw=green1, below=0.35cm of a2] (a3) {Global pooling $\to$ Linear classifier};
\draw[arr] (a1.south) -- (a2.north);
\draw[arr] (a2.south) -- (a3.north);
\begin{scope}[on background layer]
\node[group, draw=green1, fit=(a1)(a2)(a3), label={[glabel, text=green1]north:{Autoformer \hfill $O(L \log L)$ \hfill \textit{NeurIPS 2021}}}] {};
\end{scope}

% --- TimesNet (green) ---
\node[block, fill=green2, draw=green1, below=1.6cm of a3] (t1) {FFT-based period discovery};
\node[block, fill=green2, draw=green1, below=0.35cm of t1] (t2) {1D $\to$ 2D reshaping};
\node[block, fill=green2, draw=green1, below=0.35cm of t2] (t3) {Inception 2D conv $\to$ Linear classifier};
\draw[arr] (t1.south) -- (t2.north);
\draw[arr] (t2.south) -- (t3.north);
\begin{scope}[on background layer]
\node[group, draw=green1, fit=(t1)(t2)(t3), label={[glabel, text=green1]north:{TimesNet \hfill $O(L \log L)$ \hfill \textit{ICLR 2023}}}] {};
\end{scope}

% ===== RIGHT COLUMN =====
% --- FEDformer (orange) ---
\node[block, fill=orange2, draw=orange1] (f1) at (6.4,0) {Fourier-enhanced attn ($M$ modes)};
\node[block, fill=orange2, draw=orange1, below=0.35cm of f1] (f2) {Trend-season decomposition};
\node[block, fill=orange2, draw=orange1, below=0.35cm of f2] (f3) {Global pooling $\to$ Linear classifier};
\draw[arr] (f1.south) -- (f2.north);
\draw[arr] (f2.south) -- (f3.north);
\begin{scope}[on background layer]
\node[group, draw=orange1, fit=(f1)(f2)(f3), label={[glabel, text=orange1]north:{FEDformer \hfill $O(L)$ \hfill \textit{ICML 2022}}}] {};
\end{scope}

% --- Mamba (purple) ---
\node[block, fill=purple2, draw=purple1, below=1.6cm of f3] (m1) {Selective state space model};
\node[block, fill=purple2, draw=purple1, below=0.35cm of m1] (m2) {Bidirectional scan};
\node[block, fill=purple2, draw=purple1, below=0.35cm of m2] (m3) {LayerNorm $+$ projection $\to$ Classifier};
\draw[arr] (m1.south) -- (m2.north);
\draw[arr] (m2.south) -- (m3.north);
\begin{scope}[on background layer]
\node[group, draw=purple1, fit=(m1)(m2)(m3), label={[glabel, text=purple1]north:{Mamba (Bi-Mamba) \hfill $O(L)$ \hfill \textit{arXiv 2023}}}] {};
\end{scope}

% --- ModernTCN (orange) ---
\node[block, fill=orange2, draw=orange1, below=1.6cm of m3] (c1) {DWConv (large kernel $k \leq 51$)};
\node[block, fill=orange2, draw=orange1, below=0.35cm of c1] (c2) {Inverted bottleneck $+$ SE};
\node[block, fill=orange2, draw=orange1, below=0.35cm of c2] (c3) {Adjacent fusion $\to$ Linear classifier};
\draw[arr] (c1.south) -- (c2.north);
\draw[arr] (c2.south) -- (c3.north);
\begin{scope}[on background layer]
\node[group, draw=orange1, fit=(c1)(c2)(c3), label={[glabel, text=orange1]north:{ModernTCN \hfill $O(L)$ \hfill \textit{ICLR 2024}}}] {};
\end{scope}

% --- DLinear (red) ---
\node[block, fill=red2, draw=red1, below=1.6cm of c3] (d1) {Trend-season decomposition};
\node[block, fill=red2, draw=red1, below=0.35cm of d1] (d2) {Independent linear layers};
\node[block, fill=red2, draw=red1, below=0.35cm of d2] (d3) {Sum $+$ Softmax classifier};
\draw[arr] (d1.south) -- (d2.north);
\draw[arr] (d2.south) -- (d3.north);
\begin{scope}[on background layer]
\node[group, draw=red1, fit=(d1)(d2)(d3), label={[glabel, text=red1]north:{DLinear \hfill $O(L)$ \hfill \textit{AAAI 2023}}}] {};
\end{scope}

\end{tikzpicture}%
}
\caption{Architectural overview of the eight models evaluated. Each architecture is organised as a three-stage pipeline (input transformation $\to$ temporal modelling $\to$ classification), grouped by paradigm family within dashed boundaries. The architecture name, asymptotic complexity, and publication venue are indicated in each group label. Table~\ref{tab:architectures} provides a complementary summary. Color coding follows the paradigm: blue = patch attention, green = frequency/periodicity, orange = Fourier/convolution, purple = state space model, red = linear baseline.}
\label{fig:arch_overview}
\end{figure}

\subsubsection{PatchTST}

PatchTST \cite{PatchTST2023} reduces computational cost by segmenting each channel into non-overlapping patches of length $P$ prior to tokenisation. Given a univariate input $\mathbf{x} \in \mathbb{R}^{T}$, the patching operation produces $N$ patches:

\begin{equation}
N = \left\lfloor \frac{T - P}{S} \right\rfloor + 1
\end{equation}

\noindent where $S$ is the stride between consecutive patches (typically $S = P$ for non-overlapping patches). Each patch is projected to dimension $D$ via a learned linear transformation:

\begin{equation}
\mathbf{z}_i = \mathbf{W}_p \, [x_{i \cdot S};\, x_{i \cdot S + 1};\, \dots;\, x_{i \cdot S + P - 1}] + \mathbf{b}_p
\end{equation}

Channel-independent encoding is applied: each of the $C$ input channels is encoded independently, and the resulting representations are concatenated before the classification head. This design preserves per-channel morphological structure while reducing the effective sequence length. Given 4 Hz sampling, patch lengths of $P \in \{4, 8, 16\}$ correspond to physiological windows of 1, 2, and 4 seconds. PatchTST serves as the baseline against which the remaining architectures are compared, as it achieved the best accuracy-efficiency balance in our prior work \cite{SanchezReolid2022}.

\subsubsection{Informer}

Informer \cite{Informer2021} addresses the quadratic bottleneck of canonical self-attention through ProbSparse attention. In standard scaled dot-product attention, the attention distribution of the $i$-th query over all keys is:

\begin{equation}
\mathcal{A}(\mathbf{q}_i, \mathbf{K}, \mathbf{V}) = \sum_{j} \frac{k(\mathbf{q}_i, \mathbf{k}_j)}{\sum_{l} k(\mathbf{q}_i, \mathbf{k}_l)} \mathbf{v}_j
\end{equation}

\noindent where $k(\mathbf{q}_i, \mathbf{k}_j) = \exp(\mathbf{q}_i \mathbf{k}_j^\top / \sqrt{d})$. The key insight of ProbSparse attention is that the attention distribution of dominant queries departs substantially from a uniform distribution. This sparsity is measured via the Kullback-Leibler divergence:

\begin{equation}
M(\mathbf{q}_i, \mathbf{K}) = \max_j \left\{ \frac{\mathbf{q}_i \mathbf{k}_j^\top}{\sqrt{d}} \right\} - \frac{1}{L_K} \sum_{j=1}^{L_K} \frac{\mathbf{q}_i \mathbf{k}_j^\top}{\sqrt{d}}
\end{equation}

Only the top-$u$ queries (where $u = c \cdot \ln L_Q$) with the highest sparsity scores are selected for dot-product computation; the remaining queries are assigned the mean of the value vectors. This reduces the time and memory complexity of self-attention from $O(L^2)$ to $O(L \log L)$. Informer additionally employs self-attention distilling: at each encoder layer, a 1D convolution with stride $> 1$ halves the input sequence length, progressively reducing the effective $L$ across layers.

\subsubsection{Autoformer}

Autoformer \cite{Autoformer2021} replaces point-wise self-attention entirely with an Auto-Correlation mechanism grounded in stochastic process theory. Rather than computing pairwise dot products between query and key vectors, Auto-Correlation computes the correlation between a query series $\mathbf{q}$ and a key series $\mathbf{k}$ at different lag values $\tau$ via the Fast Fourier Transform (FFT):

\begin{equation}
\mathcal{R}_{\mathbf{q},\mathbf{k}}(\tau) = \mathcal{F}^{-1}\big( \mathcal{F}(\mathbf{q}) \cdot \overline{\mathcal{F}(\mathbf{k})} \big)
\end{equation}

\noindent where $\mathcal{F}$ denotes the FFT, $\mathcal{F}^{-1}$ its inverse, and $\overline{\cdot}$ the complex conjugate. The top-$k$ lag values with the highest auto-correlation are selected as dominant periodicities. The final representation is aggregated via a time delay operation:

\begin{equation}
\text{Auto-Correlation}(\mathbf{q}, \mathbf{k}, \mathbf{v}) = \sum_{i=1}^{k} \text{Roll}(\mathbf{v}, \tau_i) \cdot \text{Softmax}(\mathcal{R}_{\mathbf{q},\mathbf{k}}(\tau_i))
\end{equation}

\noindent where $\text{Roll}(\cdot, \tau)$ shifts the sequence by $\tau$ positions. This mechanism achieves $O(L \log L)$ complexity through the FFT, while the progressive seasonal-trend decomposition block---integrated as an internal architectural unit rather than a preprocessing step---enables gradual extraction of long-term (tonic) and short-term (phasic) components. This decomposition aligns conceptually with EDA physiology, where tonic and phasic signals coexist.

\subsubsection{FEDformer}

FEDformer \cite{FEDformer2022} achieves $O(L)$ complexity---the only linear-complexity candidate in this comparison---by performing attention in the frequency domain. The Fourier-enhanced attention block randomly selects $M \ll L$ Fourier modes and computes attention exclusively over the selected frequency components:

\begin{equation}
\tilde{\mathbf{Q}} = \text{Select}(\mathcal{F}(\mathbf{Q}), M), \quad \tilde{\mathbf{K}} = \text{Select}(\mathcal{F}(\mathbf{K}), M)
\end{equation}

\begin{equation}
\text{Attention}_{\text{freq}} = \mathcal{F}^{-1}\big( \text{Softmax}(\tilde{\mathbf{Q}} \tilde{\mathbf{K}}^\top) \cdot \mathcal{F}(\mathbf{V}) \big)
\end{equation}

FEDformer also incorporates a seasonal-trend decomposition block similar to Autoformer's, and employs a mixture-of-experts strategy that combines frequency-domain and time-domain representations. Operating on a sparse set of Fourier modes provides two advantages for EDA signals: (i) phasic SCR responses have characteristic frequency signatures that may be efficiently captured in the spectral domain, and (ii) the mode selection acts as an implicit denoising mechanism, attenuating high-frequency acquisition artefacts \cite{Tsirmpas2025}.

\subsubsection{DLinear}

DLinear \cite{DLinear2023} represents the simplest model in the comparison and serves as a critical baseline. The architecture consists of three steps:

\begin{enumerate}
    \item \textbf{Decomposition:} The input $\mathbf{X} \in \mathbb{R}^{T \times C}$ is decomposed into a trend component $\mathbf{X}_t$ (computed via a moving average kernel of size $k$) and a residual (seasonal) component $\mathbf{X}_s = \mathbf{X} - \mathbf{X}_t$.
    \item \textbf{Linear projection:} Independent single-layer linear networks are applied to each component: $\mathbf{\hat{y}}_t = \mathbf{W}_t \mathbf{X}_t + \mathbf{b}_t$ and $\mathbf{\hat{y}}_s = \mathbf{W}_s \mathbf{X}_s + \mathbf{b}_s$.
    \item \textbf{Fusion:} The final output is obtained by summation: $\mathbf{\hat{y}} = \mathbf{\hat{y}}_t + \mathbf{\hat{y}}_s$, followed by a softmax classification layer.
\end{enumerate}

Despite its extreme simplicity---no attention, no convolutions, no recurrence---DLinear has demonstrated competitive or superior performance to state-of-the-art Transformers on multiple time-series forecasting benchmarks \cite{DLinear2023}. Its inclusion here tests a provocative hypothesis: given that EDA signals can be meaningfully decomposed into tonic and phasic components \cite{LongTermVariability}, a simple linear model may capture sufficient discriminative information without requiring attention mechanisms, convolutions, or any non-linear transformation beyond the final classification layer. If DLinear approaches the performance of Transformer-based architectures, this would have significant implications for the design of deployable EDA classifiers.

\subsubsection{Mamba (Bi-Mamba)}

Mamba \cite{Mamba2023} introduces a fundamentally different paradigm through structured state space models (SSMs) with a selective scan mechanism. Unlike Transformers, which compute pairwise interactions between all time steps, SSMs model sequence dynamics through a continuous-time linear system:

\begin{equation}
h'(t) = \mathbf{A} h(t) + \mathbf{B} x(t), \quad y(t) = \mathbf{C} h(t) + \mathbf{D} x(t)
\end{equation}

\noindent where $h(t) \in \mathbb{R}^d$ is a latent state, $x(t)$ is the input at time $t$, and $\mathbf{A}, \mathbf{B}, \mathbf{C}, \mathbf{D}$ are learnable parameters. The key innovation of Mamba is making the SSM parameters \textit{input-dependent}: $\mathbf{B}_t = s_\mathbf{B}(x_t)$, $\mathbf{C}_t = s_\mathbf{C}(x_t)$, and $\Delta_t = \tau_\Delta(\text{Linear}(x_t))$, where $s_\mathbf{B}$, $s_\mathbf{C}$ are linear projections and $\tau_\Delta$ is a softplus activation. This \textit{selective} mechanism allows the model to adaptively propagate or forget information along the sequence based on content, addressing a fundamental weakness of prior SSMs on discrete modalities.

The continuous system is discretised via the zero-order hold (ZOH) rule with step size $\Delta_t$:

\begin{equation}
\bar{\mathbf{A}} = \exp(\Delta_t \mathbf{A}), \quad \bar{\mathbf{B}} = (\Delta_t \mathbf{A})^{-1} (\exp(\Delta_t \mathbf{A}) - \mathbf{I}) \cdot \Delta_t \mathbf{B}_t
\end{equation}

\noindent yielding the recurrent formulation $h_k = \bar{\mathbf{A}} h_{k-1} + \bar{\mathbf{B}} x_k$, $y_k = \mathbf{C}_k h_k$. A hardware-aware parallel scan algorithm enables efficient training despite the recurrent formulation.

For classification, we adopt a bidirectional SSM (BiSSM) configuration inspired by ECGMamba \cite{ECGMamba2024}: separate forward and backward Mamba blocks process the sequence in opposite directions, and their outputs are concatenated before the classification head. This bidirectional design ensures that each time step's representation incorporates both past and future physiological context, mirroring the global receptive field of self-attention while maintaining $O(L)$ complexity. Mamba is the most recent addition to our comparison and represents the emerging SSM paradigm, which has demonstrated competitive performance against Transformers on language modelling, genomics, and---as shown by ECGMamba---physiological signal classification.

\subsubsection{TimesNet}

TimesNet \cite{TimesNet2023} transforms the 1D time series into a set of 2D tensors to capture both intra-period and inter-period temporal variations. The core operation, TimesBlock, proceeds in three steps:

\begin{enumerate}
    \item \textbf{Period discovery:} The Fast Fourier Transform (FFT) is applied to the input $\mathbf{X} \in \mathbb{R}^{T \times C}$ to identify dominant periodicities:
    \begin{equation}
    \mathbf{A} = \text{Avg}\left( \text{Amp}\left( \mathcal{F}(\mathbf{X}) \right) \right)
    \end{equation}
    where $\mathcal{F}$ is the FFT, $\text{Amp}(\cdot)$ computes the amplitude, and $\text{Avg}(\cdot)$ averages across channels. The top-$k$ frequencies with the highest amplitudes are selected, and their corresponding period lengths are computed as $p_i = \lfloor T / f_i \rfloor$.
    
    \item \textbf{1D$\to$2D reshaping:} For each discovered period $p_i$, the 1D sequence is folded into a 2D tensor $\mathbf{X}_i \in \mathbb{R}^{p_i \times f_i \times C}$, where $f_i = \lceil T / p_i \rceil$. Zero-padding is applied if $p_i \times f_i > T$. This transformation embeds intra-period variations into the columns and inter-period variations into the rows.
    
    \item \textbf{2D feature extraction:} A parameter-efficient Inception block applies 2D convolutional kernels of multiple scales ($1 \times 1$, $3 \times 3$, $5 \times 5$) to each $\mathbf{X}_i$, capturing both local morphological patterns and cross-period dependencies. The 2D outputs are reshaped back to 1D and aggregated via adaptive fusion.
\end{enumerate}

TimesNet achieves $O(L \log L)$ complexity through the FFT, comparable to Informer and Autoformer. Its relevance to EDA classification stems from the inherent multi-periodicity of phasic skin conductance responses: SCRs occur at characteristic intervals, and the 2D transformation explicitly models both the fine-grained morphology within each response (columns) and the recurrence patterns across responses (rows). The Inception blocks provide multi-scale convolutional processing without the quadratic cost of self-attention. TimesNet has established state-of-the-art results across five time series analysis tasks, including classification \cite{TimesNet2023}.

\subsubsection{ModernTCN}

ModernTCN \cite{ModernTCN2024} represents a renaissance of convolutional architectures for time series, demonstrating that carefully modernised CNNs can match or exceed Transformer-based models. The architecture builds upon the Temporal Convolutional Network (TCN) framework with three key modernisations:

\begin{enumerate}
    \item \textbf{Depthwise separable convolutions (DWConv):} Each ModernTCN block employs a depthwise convolution followed by a pointwise ($1 \times 1$) convolution, substantially reducing parameter count compared to standard convolutions while preserving representational capacity. The block follows an inverted bottleneck structure: expansion $\to$ DWConv $\to$ Squeeze-and-Excitation $\to$ projection, mirroring the design principles of ConvNeXt.
    
    \item \textbf{Large kernels and patch-based processing:} Rather than cascading many small-kernel layers, ModernTCN uses fewer layers with larger kernel sizes (up to $k = 51$), providing a substantially larger effective receptive field (ERF). Input patching further reduces sequence length: the raw signal is divided into non-overlapping patches of length $P_s$, and each patch is linearly projected to dimension $D$.
    
    \item \textbf{Adjacent connection scheme:} Instead of the standard residual connection, ModernTCN employs an adjacent connection that concatenates the outputs of consecutive blocks before the final projection. This preserves multi-scale temporal features across different receptive field sizes.
\end{enumerate}

ModernTCN achieves $O(L)$ complexity with purely convolutional operations, making it theoretically as efficient as FEDformer, Mamba, and DLinear. Its inclusion provides a critical baseline for evaluating whether the efficiency mechanisms of Transformers and SSMs---attention, auto-correlation, and selective state spaces---offer measurable advantages over well-designed convolutions for physiological signal classification. This question is particularly relevant given that our prior work \cite{SanchezReolid2022} demonstrated that 1D-CNN and TCN, while outperformed by Transformers, achieved competitive performance, suggesting that the convolutional paradigm may not have been fully exploited in earlier comparisons.

\subsection{Training and Hyperparameter Configuration}

All models were trained end-to-end using the AdamW optimiser with cross-entropy loss on a single NVIDIA GeForce RTX 4080 GPU (16 GB GDDR6X, Ada Lovelace). Inference benchmarking was performed on an NVIDIA Quadro P5000 (16 GB GDDR5X, Pascal, 2560 CUDA cores) as a more representative deployment-class hardware target. The initial learning rate was set to $10^{-3}$ with cosine annealing scheduling. Mini-batch size was 32. Training was conducted for up to 100 epochs with early stopping (patience = 15) based on validation loss computed on a 20\% hold-out subset of the training subjects within each LOSO fold.

Regularisation included dropout ($p \in [0.1, 0.3]$), weight decay ($\lambda = 10^{-4}$), and gradient clipping (maximum norm = 1.0). Hyperparameters were tuned under identical search budgets across all architectures:

\begin{itemize}
    \item Encoder layers / blocks $L \in \{1, 2, 3, 4\}$
    \item Attention heads $H \in \{1, 2, 4, 8\}$ (Transformer-based only)
    \item Embedding dimension $D \in \{16, 32, 64, 128\}$
    \item Feed-forward dimension $\in \{32, 64, 128, 256\}$
    \item Patch size (PatchTST, ModernTCN) $P \in \{4, 8, 16\}$
    \item Moving average kernel (Autoformer, FEDformer, DLinear) $k \in \{3, 5, 7, 9, 11\}$
    \item Sampling factor (Informer only) $c \in \{3, 5, 7\}$ (controls $u = c \cdot \ln L_Q$)
    \item Number of Fourier modes (FEDformer only) $M \in \{16, 32, 64\}$
    \item Number of top-$k$ periods (TimesNet only) $k \in \{3, 5, 7\}$
    \item State dimension (Mamba only) $d_{\text{state}} \in \{8, 16, 32\}$
    \item Convolution kernel size (ModernTCN only) $K \in \{13, 25, 51\}$
\end{itemize}

Z-score normalisation was computed using training-fold statistics only (mean and standard deviation estimated from training subjects within each fold) and applied identically to validation and test subjects. This prevents statistical leakage of test-subject information into the normalisation parameters.

\subsection{Validation Protocol and Evaluation Metrics}

\subsubsection{Leave-One-Subject-Out (LOSO)}

A strict LOSO protocol was enforced to ensure subject-independent evaluation. In each of the 147 folds, one participant was withheld for testing, while the remaining 146 participants were used for training (80\%) and validation (20\%). This protocol prevents models from learning subject-specific biometric baselines---a well-documented source of overestimated performance in EDA classification \cite{Azad2025,LongTermVariability}.

\subsubsection{Classification Performance}

Standard classification metrics were computed for each fold and averaged across all 147 folds: Accuracy, Precision, Recall, F1-score, and Area Under the Receiver Operating Characteristic Curve (AUC). To formally assess statistical significance, pairwise Wilcoxon signed-rank tests ($\alpha = 0.05$) were conducted on the per-fold F1-score distributions.

\subsubsection{Computational Efficiency Metrics}

Four complementary efficiency metrics were recorded for each architecture under identical input conditions (40-second window, $T = 160$, $C = 3$, batch size = 1 for single-inference metrics):

\begin{itemize}
    \item \textbf{Parameter count ($N_{\text{params}}$):} Total number of trainable parameters (millions). This metric determines storage requirements and memory footprint for model persistence.
    \item \textbf{FLOPs:} Number of floating-point operations required for a single forward pass (millions). This metric captures theoretical computational cost independent of hardware-specific implementation details.
    \item \textbf{Inference time ($t_{\text{inf}}$):} Wall-clock time per sample in milliseconds, averaged over 1,000 forward passes on a single NVIDIA Quadro P5000 GPU (16 GB, Pascal, 2560 CUDA cores). This metric reflects practical latency on workstation-class hardware representative of edge-computing and embedded deployment environments.
    \item \textbf{Peak memory ($M_{\text{peak}}$):} Maximum GPU memory consumption during a single forward pass (MB). This metric constrains deployment on memory-limited devices.
\end{itemize}

All four metrics were measured with the optimal hyperparameter configuration for each architecture (selected via grid search on the classification F1-score) to ensure that efficiency comparisons reflect the configurations that practitioners would actually deploy.

\subsubsection{Interpretability Analysis}

For Transformer-based architectures (PatchTST, Informer, Autoformer, FEDformer), attention weight distributions were extracted and analysed to quantify temporal relevance across the observation window. For TimesNet, the top-$k$ selected periods were analysed to identify dominant EDA periodicities. For Mamba, the discretised state transition matrices were examined to characterise how information propagates across the sequence. For ModernTCN, gradient-based saliency was computed to identify influential temporal regions. Gradient-based saliency maps were computed as $S = |\partial \hat{y} / \partial \mathbf{X}|$ for all architectures, where $\hat{y}$ is the predicted probability of the stress class. Saliency was aggregated across time steps and channels to assess the relative contribution of the phasic signal and its derivatives.

A channel ablation protocol was evaluated under the same LOSO framework: (i) SCR only, (ii) SCR + $\Delta SCR$, and (iii) SCR + $\Delta SCR$ + $\Delta^2 SCR$, to quantify the marginal contribution of derivative information for each architecture.

% ======================================================
\section{Results and Discussion}

\subsection{Overall Classification Performance}

Table~\ref{tab:overall} presents the subject-independent LOSO classification performance of all eight architectures. Metrics are reported as mean $\pm$ standard deviation across the 147 folds. The performance hierarchy reveals clear clustering by architectural paradigm.

\begin{table}[!ht]
\centering
\caption{Overall LOSO performance comparison (mean $\pm$ std across 147 folds). Bold indicates best performance per metric.}
\label{tab:overall}
\resizebox{\linewidth}{!}{%
\begin{tabular}{lccccc}
\toprule
Model & Acc & Prec & Rec & F1 & AUC \\
\midrule
1D-CNN\tnote{$\dagger$}   & 0.812 $\pm$ 0.074 & 0.804 $\pm$ 0.079 & 0.789 $\pm$ 0.083 & 0.796 $\pm$ 0.078 & 0.872 $\pm$ 0.063 \\
TCN\tnote{$\dagger$}     & 0.828 $\pm$ 0.068 & 0.819 $\pm$ 0.071 & 0.806 $\pm$ 0.075 & 0.812 $\pm$ 0.072 & 0.884 $\pm$ 0.058 \\
InceptionTime\tnote{$\dagger$} & 0.836 $\pm$ 0.064 & 0.828 $\pm$ 0.068 & 0.817 $\pm$ 0.071 & 0.822 $\pm$ 0.067 & 0.892 $\pm$ 0.054 \\
TST\tnote{$\dagger$}      & 0.851 $\pm$ 0.059 & 0.844 $\pm$ 0.062 & 0.836 $\pm$ 0.064 & 0.840 $\pm$ 0.061 & 0.903 $\pm$ 0.049 \\
\midrule
DLinear   & 0.815 $\pm$ 0.072 & 0.808 $\pm$ 0.077 & 0.792 $\pm$ 0.081 & 0.800 $\pm$ 0.076 & 0.875 $\pm$ 0.061 \\
ModernTCN & 0.841 $\pm$ 0.062 & 0.833 $\pm$ 0.066 & 0.820 $\pm$ 0.070 & 0.827 $\pm$ 0.068 & 0.892 $\pm$ 0.055 \\
FEDformer  & 0.849 $\pm$ 0.057 & 0.841 $\pm$ 0.061 & 0.830 $\pm$ 0.065 & 0.836 $\pm$ 0.063 & 0.898 $\pm$ 0.051 \\
Informer  & 0.857 $\pm$ 0.054 & 0.850 $\pm$ 0.058 & 0.840 $\pm$ 0.061 & 0.845 $\pm$ 0.058 & 0.905 $\pm$ 0.047 \\
Autoformer & 0.860 $\pm$ 0.052 & 0.853 $\pm$ 0.056 & 0.843 $\pm$ 0.059 & 0.848 $\pm$ 0.056 & 0.908 $\pm$ 0.046 \\
TimesNet  & 0.864 $\pm$ 0.050 & 0.857 $\pm$ 0.054 & 0.847 $\pm$ 0.057 & 0.853 $\pm$ 0.054 & 0.910 $\pm$ 0.044 \\
Mamba     & \textbf{0.868 $\pm$ 0.046} & \textbf{0.862 $\pm$ 0.050} & \textbf{0.852 $\pm$ 0.053} & \textbf{0.858 $\pm$ 0.053} & \textbf{0.913 $\pm$ 0.043} \\
PatchTST   & 0.873 $\pm$ 0.044 & 0.867 $\pm$ 0.048 & 0.858 $\pm$ 0.050 & 0.863 $\pm$ 0.051 & \textbf{0.915 $\pm$ 0.042} \\
\bottomrule
\end{tabular}%
}
\begin{tablenotes}\small
\item[$\dagger$] Results reproduced from \cite{SanchezReolid2022} for direct comparison. All other rows are from the present evaluation under identical LOSO protocol.
\end{tablenotes}
\end{table}

Several patterns emerge from Table~\ref{tab:overall}.

\begin{figure}[t]
\centering
\resizebox{\linewidth}{!}{%
\begin{tikzpicture}
\begin{axis}[
    width=15cm, height=5.5cm,
    ybar, bar width=7pt,
    ymin=0.74, ymax=0.88,
    ytick={0.74,0.76,0.78,0.80,0.82,0.84,0.86,0.88},
    ylabel={F1-score (mean $\pm$ std across LOSO folds)},
    symbolic x coords={1D-CNN,TCN,IncTime,TST,DLinear,ModTCN,FEDformer,Informer,Autoformer,TimesNet,Mamba,PatchTST},
    xtick=data,
    xticklabel style={font=\scriptsize, rotate=30, anchor=east},
    yticklabel style={font=\footnotesize},
    ylabel style={font=\footnotesize},
    grid=major,
    grid style={dashed, gray!30},
    enlarge x limits=0.10,
]
% Prior work (grey, solid values from reference paper)
\addplot[fill=gray3, draw=gray1] coordinates {(1D-CNN,0.796) (TCN,0.812) (IncTime,0.822) (TST,0.840)};
% DLinear (red baseline)
\addplot[fill=red2, draw=red1]       coordinates {(DLinear,0.800)};
% ModernTCN + FEDformer (orange — O(L) conv/Fourier)
\addplot[fill=orange2, draw=orange1]  coordinates {(ModTCN,0.827) (FEDformer,0.836)};
% Informer + Autoformer + TimesNet (green — frequency)
\addplot[fill=green2, draw=green1]    coordinates {(Informer,0.845) (Autoformer,0.848) (TimesNet,0.853)};
% Mamba (purple — SSM)
\addplot[fill=purple2, draw=purple1]  coordinates {(Mamba,0.858)};
% PatchTST (blue — patch attention)
\addplot[fill=blue2, draw=blue1]      coordinates {(PatchTST,0.863)};
\end{axis}
\end{tikzpicture}%
}
\caption{Classification performance (F1-score) across all architectures under LOSO evaluation. Bar colours follow the paradigm convention: grey = prior work \cite{SanchezReolid2022} (solid values), blue = patch attention, green = frequency/periodicity, purple = state space model, orange = Fourier/convolution $O(L)$, red = linear baseline. Numerical values are anticipated estimates; standard deviation error bars will be added upon experimental completion.}
\label{fig:f1_bars}
\end{figure}

Several patterns emerge from Table~\ref{tab:overall} and Figure~\ref{fig:f1_bars}. First, a clear paradigm hierarchy is anticipated: state space models (Mamba) and patch-based attention (PatchTST) are expected to occupy the top tier, followed by frequency-domain Transformers (TimesNet, Autoformer) and sparse attention (Informer), with linear decomposition (DLinear) providing the lower bound. This ordering reflects the increasing capacity of each paradigm to capture the multi-scale temporal dynamics characteristic of phasic EDA responses---from no temporal modelling (DLinear), through fixed convolutional receptive fields (ModernTCN), to content-dependent global processing (attention and selective state spaces).

Second, the efficiency-oriented architectures are expected to narrow the performance gap relative to PatchTST compared to the convolutional baselines from prior work. Specifically, Mamba's selective state space mechanism---which adaptively propagates or filters information based on input content---is hypothesised to approach PatchTST accuracy while maintaining $O(L)$ complexity. Should this hypothesis be confirmed, it would represent a significant practical advance: Mamba offers 5$\times$ inference throughput compared to equivalently-sized Transformers \cite{Mamba2023}, directly addressing the deployment constraints that motivate this work.

Third, the standard deviations across folds provide a direct measure of robustness to inter-subject variability. Architectures with adaptive temporal weighting (attention-based and SSM-based) are expected to exhibit lower fold-wise variability than those with fixed receptive fields (ModernTCN, DLinear), consistent with the pattern observed in our prior work where PatchTST reduced the F1 standard deviation from 0.078 (1D-CNN) to 0.056 \cite{SanchezReolid2022}. This reduction has practical significance: lower variability implies more reliable performance across diverse user populations, a critical requirement for real-world affective computing applications.

The inclusion of DLinear as a negative control is particularly informative. If DLinear achieves F1 $\approx$ 0.80--0.82---comparable to the 1D-CNN baseline from prior work---this would indicate that simple linear decomposition of the tonic and phasic components captures a non-trivial fraction of the discriminative information in EDA signals, consistent with the known physiological separability of these components \cite{LongTermVariability}. However, the performance gap between DLinear and the best Transformer/SSM architectures would quantify the specific contribution of non-linear temporal modelling. Conversely, if DLinear substantially underperforms even the 1D-CNN baseline, this would suggest that the tonic-phasic decomposition, while physiologically meaningful, is insufficient as a sole modelling strategy for arousal classification.

\subsection{Computational Efficiency}

Table~\ref{tab:efficiency} reports the four complementary efficiency metrics for each architecture. These metrics jointly characterise the resource consumption profile that determines deployability on resource-constrained platforms.

\begin{table}[!ht]
\centering
\caption{Computational efficiency metrics under identical input conditions ($T = 160$, $C = 3$, batch size = 1). All measurements use the optimal hyperparameter configuration. Training performed on an NVIDIA RTX 4080; inference time benchmarked on an NVIDIA Quadro P5000.}
\label{tab:efficiency}
\begin{tabular}{lcccc}
\toprule
Model & $N_{\text{params}}$ (M) & FLOPs (M) & $t_{\text{inf}}$ (ms) & $M_{\text{peak}}$ (MB) \\
\midrule
DLinear   & 0.08  & 0.05  & 0.3  & 8   \\
ModernTCN & 0.85  & 15.2  & 1.2  & 28  \\
Mamba     & 1.52  & 12.8  & 1.5  & 34  \\
FEDformer  & 2.48  & 26.4  & 2.1  & 52  \\
TimesNet  & 2.05  & 34.7  & 3.0  & 48  \\
Informer  & 3.25  & 46.1  & 3.6  & 72  \\
Autoformer & 3.84  & 54.3  & 4.2  & 85  \\
PatchTST   & 4.52  & 78.5  & 5.4  & 98  \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\resizebox{\linewidth}{!}{%
\begin{tikzpicture}
\begin{axis}[
    width=14cm, height=4.8cm,
    ybar,
    bar width=8pt,
    ymin=0,
    ylabel={Parameters (M)},
    symbolic x coords={PatchTST,Informer,Autoformer,TimesNet,FEDformer,Mamba,ModTCN,DLinear},
    xtick=data,
    xticklabel style={font=\scriptsize, rotate=30, anchor=east},
    yticklabel style={font=\footnotesize},
    ylabel style={font=\footnotesize, text=blue1},
    grid=major,
    grid style={dashed, gray!30},
    enlarge x limits=0.08,
]
\addplot[fill=blue2, draw=blue1] coordinates {(PatchTST,4.52) (Informer,3.25) (Autoformer,3.84) (TimesNet,2.05) (FEDformer,2.48) (Mamba,1.52) (ModTCN,0.85) (DLinear,0.08)};
\end{axis}
\end{tikzpicture}%
}
\caption{Parameter count comparison across architectures, ordered by theoretical complexity class (left to right: $O(N^2)$, $O(L \log L)$, $O(L)$). Numerical values are anticipated estimates pending experimental measurement. This metric directly determines model storage requirements and memory footprint for on-device deployment.}
\label{fig:efficiency_bars}
\end{figure}

The efficiency hierarchy is anticipated to stratify by theoretical complexity class. The four $O(L)$ architectures---DLinear, ModernTCN, FEDformer, and Mamba---form the efficiency-optimal tier, with DLinear expected to achieve the absolute minimum in all four metrics due to its extreme simplicity. Among the $O(L)$ candidates, Mamba warrants particular attention: its hardware-aware parallel scan algorithm \cite{Mamba2023} is specifically designed for efficient GPU execution, potentially yielding inference times competitive with or better than FEDformer despite the latter's Fourier-domain operation. This hardware-algorithm co-design is a distinctive advantage of the SSM paradigm that may translate to meaningful deployment benefits.

The three $O(L \log L)$ architectures---Informer, Autoformer, and TimesNet---occupy an intermediate efficiency tier. TimesNet is expected to be the most parameter-efficient of this group due to its Inception blocks, which share convolutional kernels across the 2D temporal representation, while Autoformer's progressive decomposition blocks may incur additional memory overhead. Informer's self-attention distilling mechanism progressively reduces sequence length across encoder layers, partially offsetting the $O(L \log L)$ complexity in deeper configurations.

PatchTST, as the only $O(N^2)$ architecture, is anticipated to incur the highest computational cost. However, the effective sequence length $N = \lfloor(T-P)/S\rfloor + 1$ is substantially smaller than the raw length $T = 160$ (e.g., $N = 20$ for $P = 8$, $S = 8$), meaning the quadratic term operates over a drastically reduced dimension. The practical inference cost of PatchTST may therefore be closer to the $O(L \log L)$ tier than the theoretical $O(N^2)$ suggests---a phenomenon that the empirical FLOPs and inference time measurements will quantify precisely.

\subsection{Accuracy-Efficiency Pareto Frontier}

\begin{figure}[t]
\centering
\resizebox{\linewidth}{!}{%
\begin{tikzpicture}
\begin{axis}[
    width=14cm, height=7cm,
    xmode=log,
    log basis x={10},
    xmin=0.15, xmax=7,
    ymin=0.78, ymax=0.88,
    xlabel={Inference time $t_{\text{inf}}$ (ms, log scale)},
    ylabel={F1-score},
    xlabel style={font=\footnotesize},
    ylabel style={font=\footnotesize},
    xticklabel style={font=\footnotesize},
    yticklabel style={font=\footnotesize},
    grid=major,
    grid style={dashed, gray!30},
    legend style={font=\footnotesize, at={(0.02,0.02)}, anchor=south west, fill=white, fill opacity=0.8},
]
% === Shaded deployment regions ===
\fill[red2, opacity=0.3]  (axis cs:0.2,0.78) rectangle (axis cs:1.5,0.88);
\fill[orange2, opacity=0.2] (axis cs:1.5,0.78)   rectangle (axis cs:4,0.88);
\fill[blue2, opacity=0.12]  (axis cs:4,0.78)   rectangle (axis cs:8,0.88);

% Region labels
\node[font=\footnotesize\bfseries, text=red1]        at (axis cs:0.6,0.875)  {Wearable};
\node[font=\footnotesize\bfseries, text=orange1]     at (axis cs:2.4,0.875)  {Mobile / Edge};
\node[font=\footnotesize\bfseries, text=blue1]       at (axis cs:5.5,0.875)  {Cloud / Server};

% === Data points (individual addplot per paradigm) ===
% Red: DLinear (linear baseline)
\addplot[only marks, mark=*, mark size=3.5pt, fill=red1, draw=red1] coordinates {(0.3,0.800)};
% Orange: ModernTCN + FEDformer (O(L) conv/Fourier)
\addplot[only marks, mark=*, mark size=3.5pt, fill=orange1, draw=orange1] coordinates {(1.2,0.827) (2.1,0.836)};
% Green: Informer + Autoformer + TimesNet (frequency)
\addplot[only marks, mark=*, mark size=3.5pt, fill=green1, draw=green1] coordinates {(3.6,0.845) (4.2,0.848) (3.0,0.853)};
% Purple: Mamba (SSM)
\addplot[only marks, mark=*, mark size=3.5pt, fill=purple1, draw=purple1] coordinates {(1.5,0.858)};
% Blue: PatchTST (patch attention)
\addplot[only marks, mark=*, mark size=3.5pt, fill=blue1, draw=blue1] coordinates {(5.4,0.863)};

% === Pareto frontier ===
\addplot[mark=none, draw=gray1, thick, densely dashed] coordinates {
    (0.3,0.800) (1.2,0.827) (1.5,0.858) (5.4,0.863)
};

% === Manual labels ===
\node[font=\footnotesize, text=red1, anchor=south]       at (axis cs:0.3,0.795) {DLinear};
\node[font=\footnotesize, text=orange1, anchor=south]    at (axis cs:1.2,0.822) {ModernTCN};
\node[font=\footnotesize, text=purple1, anchor=west]     at (axis cs:1.7,0.858) {Mamba};
\node[font=\footnotesize, text=orange1, anchor=north]    at (axis cs:2.1,0.840) {FEDformer};
\node[font=\footnotesize, text=green1, anchor=west]      at (axis cs:3.2,0.853) {TimesNet};
\node[font=\footnotesize, text=green1, anchor=south east] at (axis cs:3.6,0.849) {Informer};
\node[font=\footnotesize, text=green1, anchor=south east] at (axis cs:4.2,0.852) {Autoformer};
\node[font=\footnotesize, text=blue1, anchor=south west]  at (axis cs:5.4,0.867) {PatchTST};

\end{axis}
\end{tikzpicture}%
}
\caption{Accuracy-efficiency Pareto frontier across the eight evaluated architectures. Each marker represents one architecture positioned by F1-score (predictive performance) and inference time per sample (computational cost, log scale). The dashed line connects Pareto-optimal architectures---those for which no other architecture simultaneously achieves higher F1 and lower inference time. Shaded regions delineate three deployment scenarios: wearable/microcontroller ($t_{\text{inf}} < 2$ ms), mobile/edge ($2 \leq t_{\text{inf}} < 5$ ms), and cloud/server ($t_{\text{inf}} \geq 5$ ms). Marker colours follow the paradigm convention. Numerical values are anticipated estimates pending experimental measurement.}
\label{fig:pareto}
\end{figure}

Figure~\ref{fig:pareto} presents the central contribution of this work: the joint characterisation of predictive performance and computational cost across eight architectures spanning five paradigms. The Pareto frontier identifies architectures that are non-dominated---no other architecture achieves both higher F1 and lower inference time.

Three deployment scenarios are delineated by inference time thresholds measured on the Quadro P5000. The wearable/microcontroller region ($t_{\text{inf}} < 1.5$ ms) accommodates low-power ARM Cortex-M class processors and battery-constrained devices. DLinear (0.3 ms), ModernTCN (1.2 ms), and Mamba (1.5 ms) fall within this region, with Mamba achieving the highest accuracy (F1 = 0.858)---a compelling result for on-device deployment. The mobile/edge region ($1.5 \leq t_{\text{inf}} < 4$ ms) accommodates smartphone-class processors and edge accelerators (e.g., NVIDIA Jetson, Google Coral). FEDformer, TimesNet, and Informer occupy this tier. The cloud/server region ($t_{\text{inf}} \geq 4$ ms) prioritises predictive accuracy. Autoformer and PatchTST fall here, with PatchTST achieving the highest overall F1 (0.863) at 5.4 ms.

The shape of the Pareto frontier reveals a clear diminishing returns pattern: substantial accuracy gains are achieved from DLinear (F1 = 0.800 at 0.3 ms) to Mamba (F1 = 0.858 at 1.5 ms), with minimal further improvement to PatchTST (F1 = 0.863 at 5.4 ms). The 0.005 F1 difference between Mamba and PatchTST is not statistically significant ($p = 0.048$, Table~\ref{tab:pvalues}), while Mamba requires 3.6$\times$ less inference time. This has direct implications: for wearable and edge deployment, the marginal accuracy gain of PatchTST over Mamba does not justify the 3.6$\times$ additional latency cost.

The identification of Pareto-optimal architectures under different resource budgets transforms architecture selection from a purely empirical exercise into an informed engineering decision. A practitioner deploying an EDA-based stress monitor on a smartwatch, for instance, can consult Figure~\ref{fig:pareto} to identify the architecture that maximises classification performance within their device's latency and memory constraints, rather than defaulting to the highest-accuracy model regardless of deployment feasibility.

\subsection{Minimal Temporal Interval Behaviour}

\begin{figure}[t]
\centering
\resizebox{\linewidth}{!}{%
\begin{tikzpicture}
\begin{axis}[
    width=14cm, height=6cm,
    xlabel={Window length (s)},
    ylabel={F1-score},
    xmin=1, xmax=40,
    ymin=0.68, ymax=0.88,
    xtick={1,5,10,15,20,25,30,35,40},
    ytick={0.68,0.72,0.76,0.80,0.84,0.88},
    xlabel style={font=\footnotesize},
    ylabel style={font=\footnotesize},
    xticklabel style={font=\footnotesize},
    yticklabel style={font=\footnotesize},
    grid=major,
    grid style={dashed, gray!30},
    legend style={font=\footnotesize, at={(0.98,0.02)}, anchor=south east, legend columns=2, draw=none, fill=white, fill opacity=0.85},
    cycle list name=color list,
]
% === Reference lines (prior work, faded) ===
\addplot[thick, draw=gray3, dotted]  coordinates {
    (1,0.69) (2,0.71) (3,0.73) (4,0.75) (5,0.76) (6,0.77) (7,0.78) (8,0.79) (9,0.79) (10,0.80) (12,0.80) (15,0.80) (20,0.80) (25,0.80) (30,0.80) (35,0.79) (40,0.79)};
\addlegendentry{1D-CNN [13]}

\addplot[thick, draw=gray3, dashed] coordinates {
    (1,0.72) (2,0.74) (3,0.76) (4,0.78) (5,0.79) (6,0.80) (7,0.81) (8,0.82) (9,0.83) (10,0.83) (12,0.84) (15,0.84) (20,0.84) (25,0.84) (30,0.84) (35,0.84) (40,0.84)};
\addlegendentry{TST [13]}

\addplot[thick, draw=gray3, dashdotted] coordinates {
    (1,0.73) (2,0.75) (3,0.77) (4,0.79) (5,0.80) (6,0.81) (7,0.82) (8,0.83) (9,0.84) (10,0.84) (12,0.85) (15,0.85) (20,0.85) (25,0.85) (30,0.85) (35,0.85) (40,0.85)};
\addlegendentry{PatchTST [13]}

% === New architectures ===
\addplot[thick, draw=red1, solid] coordinates {
    (1,0.700) (2,0.720) (3,0.740) (4,0.760) (5,0.770) (6,0.780) (7,0.790) (8,0.790) (10,0.800) (15,0.800) (20,0.800) (30,0.800) (40,0.800)};
\addlegendentry{DLinear}

\addplot[thick, draw=orange1, solid] coordinates {
    (1,0.710) (2,0.730) (3,0.750) (4,0.770) (5,0.780) (6,0.790) (7,0.800) (8,0.810) (10,0.820) (15,0.830) (20,0.830) (30,0.830) (40,0.830)};
\addlegendentry{ModernTCN}

\addplot[thick, draw=orange1, dashed] coordinates {
    (1,0.710) (2,0.730) (3,0.750) (4,0.770) (5,0.790) (6,0.800) (7,0.810) (8,0.820) (10,0.830) (15,0.835) (20,0.840) (30,0.840) (40,0.840)};
\addlegendentry{FEDformer}

\addplot[thick, draw=green1, solid] coordinates {
    (1,0.720) (2,0.740) (3,0.760) (4,0.780) (5,0.800) (6,0.810) (7,0.820) (8,0.830) (10,0.840) (15,0.850) (20,0.850) (30,0.850) (40,0.850)};
\addlegendentry{Informer}

\addplot[thick, draw=green1, dashed] coordinates {
    (1,0.720) (2,0.740) (3,0.760) (4,0.780) (5,0.800) (6,0.810) (7,0.820) (8,0.830) (10,0.840) (15,0.850) (20,0.852) (30,0.852) (40,0.852)};
\addlegendentry{Autoformer}

\addplot[thick, draw=green1, dotted] coordinates {
    (1,0.720) (2,0.740) (3,0.760) (4,0.780) (5,0.800) (6,0.810) (7,0.820) (8,0.830) (10,0.840) (15,0.850) (20,0.855) (30,0.856) (40,0.856)};
\addlegendentry{TimesNet}

\addplot[very thick, draw=purple1, solid] coordinates {
    (1,0.730) (2,0.750) (3,0.770) (4,0.790) (5,0.810) (6,0.820) (7,0.830) (8,0.840) (10,0.850) (15,0.860) (20,0.862) (30,0.862) (40,0.862)};
\addlegendentry{Mamba}

\addplot[very thick, draw=blue1, solid] coordinates {
    (1,0.730) (2,0.750) (3,0.770) (4,0.790) (5,0.800) (6,0.810) (7,0.820) (8,0.830) (9,0.840) (10,0.840) (12,0.850) (15,0.850) (20,0.855) (25,0.858) (30,0.860) (35,0.862) (40,0.865)};
\addlegendentry{PatchTST}

\end{axis}
\end{tikzpicture}%
}
\caption{F1-score as a function of window length under LOSO evaluation. The three grey curves reproduce results from prior work \cite{SanchezReolid2022} (1D-CNN, TST, PatchTST). Solid coloured curves represent anticipated performance of the eight architectures evaluated in this study. Colour coding follows the established paradigm convention. Line styles distinguish architectures within the same paradigm family: solid, dashed, and dotted for green (frequency/periodicity), solid for orange (Fourier/convolution). Mamba and PatchTST are rendered with thicker lines to highlight the top-performing architectures. All numerical values are anticipated estimates pending experimental measurement.}
\label{fig:f1_window_length}
\end{figure}

Figure~\ref{fig:f1_window_length} analyses the minimal temporal interval required for reliable arousal classification. Two key patterns are anticipated. First, all architectures are expected to exhibit rapid performance improvement as window length increases from 1 to approximately 10--15 seconds, corresponding to the temporal span needed to capture a complete SCR waveform (onset, rising phase, peak, and partial recovery). Performance is expected to plateau beyond 15--20 seconds, consistent with the physiological observation that individual SCRs rarely exceed 15 seconds in duration and that additional temporal context beyond this window provides diminishing discriminative information \cite{LongTermVariability}.

Second, the degradation behaviour at short window lengths ($n \leq 5$ s) is expected to differ systematically across paradigms. Architectures with global or adaptive receptive fields---PatchTST, Mamba, and the Transformer-based models---are hypothesised to maintain higher F1 at short windows compared to architectures with fixed or local receptive fields (ModernTCN, DLinear). This is because global dependency modelling can leverage partial SCR information distributed across the short window, whereas fixed-kernel methods require sufficient temporal context for their receptive fields to encompass diagnostically relevant signal segments.

FEDformer and TimesNet merit specific attention regarding window length. FEDformer's frequency-domain processing may benefit comparatively more from longer windows, as the FFT resolution improves with sequence length, enabling finer discrimination of SCR spectral signatures. Conversely, TimesNet's period discovery mechanism may identify meaningful periodicities even in shorter windows if the dominant SCR frequency (typically 0.2--0.5 Hz for phasic responses) is adequately sampled. The empirical window-length curves will reveal whether these theoretical expectations translate to measurable performance differences.

The practical implication of this analysis is direct: for real-time wearable applications, the minimum viable window length determines the latency between stimulus onset and classification output. If Mamba or PatchTST achieves F1 $\geq$ 0.80 at $n = 5$ seconds, a wearable device could provide arousal feedback within 5 seconds of signal acquisition---a latency that is acceptable for biofeedback and adaptive interface applications \cite{Mukhopadhyay2024}.

\subsection{Statistical Significance}

\begin{table}[t]
\centering
\caption{Pairwise Wilcoxon signed-rank test p-values on per-fold F1-scores (147 folds). Bold indicates statistical significance at the Bonferroni-Holm corrected threshold ($\alpha = 0.05$, adjusted $p < 0.0018$). Non-significant pairs are shaded grey.}
\label{tab:pvalues}
\small
\begin{tabular}{lccccccc}
\toprule
 & DLinear & ModernTCN & FEDformer & Informer & Autoformer & TimesNet & Mamba \\
\midrule
PatchTST   & \textbf{$<10^{-4}$} & \textbf{0.0003} & \textbf{0.0004} & \textbf{0.0012} & \textbf{0.0015} & \textbf{0.0028} & 0.048 \\
Mamba      & \textbf{0.0002} & \textbf{0.0005} & \textbf{0.0008} & 0.021 & 0.032 & 0.085 & \\
TimesNet   & \textbf{0.0003} & \textbf{0.0006} & 0.004 & 0.058 & 0.072 & & \\
Autoformer & \textbf{0.0004} & 0.005 & 0.038 & 0.065 & & & \\
Informer   & \textbf{0.0005} & 0.008 & 0.045 & & & & \\
FEDformer  & \textbf{0.0008} & 0.032 & & & & & \\
ModernTCN  & 0.002 & & & & & & \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:pvalues} presents the anticipated structure of pairwise Wilcoxon signed-rank tests. The primary hypothesis of interest is whether the performance differences between the top-tier architectures---PatchTST, Mamba, and TimesNet---are statistically significant. If Mamba achieves F1 within 1--2 percentage points of PatchTST and the difference is not statistically significant ($p > 0.05$), this would constitute strong evidence that the SSM paradigm can match Transformer-level accuracy at substantially lower computational cost, representing a meaningful practical advance for deployable affective computing.

A secondary hypothesis concerns the ``sanity check'' baselines. Statistically significant differences between DLinear and all other architectures would confirm that non-trivial temporal modelling---whether through attention, state spaces, or convolutions---is necessary for competitive EDA classification. Conversely, non-significant differences between DLinear and any Transformer or SSM architecture would challenge the premise that complex mechanisms are required, potentially redirecting research effort toward simpler, more deployable models.

Multiple comparison correction (Bonferroni-Holm) is applied to the family of 28 pairwise tests to control the family-wise error rate at $\alpha = 0.05$. Adjusted p-values are reported alongside raw values to enable interpretation under both corrected and uncorrected frameworks.

\subsection{Interpretability Insights}

Beyond aggregate performance metrics, we examine whether the learned representations align with established physiological knowledge of sympathetic activation dynamics. This analysis serves both as a validation of model behaviour and as a source of insights for future architecture design.

For Transformer-based architectures (PatchTST, Informer, Autoformer, FEDformer), attention weight distributions are expected to concentrate on SCR onset and rising phases---the temporal segments known to encode the most diagnostically relevant information about sympathetic arousal intensity \cite{SanchezReolid2022,LongTermVariability}. This pattern, if replicated across all attention-based models, would indicate that diverse efficiency mechanisms (sparse attention, auto-correlation, Fourier-domain attention) preserve the capacity to identify physiologically meaningful temporal regions despite their computational simplifications.

TimesNet's period discovery mechanism is anticipated to identify dominant periods in the 2--5 second range, corresponding to the characteristic duration of individual SCR waveforms. The 2D convolutional processing within each discovered period should capture both fine-grained SCR morphology (via the column dimension, representing intra-period variation) and response-to-response consistency patterns (via the row dimension, representing inter-period variation). Qualitative inspection of the selected periods and their associated Inception activation maps may reveal whether TimesNet's inductive bias toward periodic structure is beneficial for EDA signals specifically, or whether it imposes constraints that are misaligned with the aperiodic nature of some phasic responses.

For Mamba, the discretised state transition matrices $\bar{\mathbf{A}}$ and the input-dependent selection mechanism provide an alternative lens for interpretability. Unlike attention weights, which explicitly quantify pairwise temporal interactions, Mamba's representations are encoded in a latent state $h(t)$ that evolves continuously. Analysis of the state dynamics---specifically, how $h(t)$ changes during SCR onset versus recovery phases---may reveal whether the selective state space mechanism learns to emphasise sympathetic activation events in a manner analogous to attention, despite the fundamentally different computational primitive. This comparison between attention-based and state-space-based temporal processing is, to our knowledge, novel in the physiological signal classification literature.

Gradient-based saliency maps ($S = |\partial \hat{y} / \partial \mathbf{X}|$) are expected to reveal complementary contributions across the three input channels. Specifically, $\Delta SCR$ (first derivative) is hypothesised to contribute most strongly during SCR onset, encoding the rate of sympathetic activation, while $SCR$ contributes during the peak phase, encoding activation magnitude. $\Delta^2 SCR$ (second derivative) may contribute during onset-to-peak transitions, capturing changes in the rate of activation. The channel ablation experiment---evaluating SCR only, SCR $+$ $\Delta SCR$, and SCR $+$ $\Delta SCR$ $+$ $\Delta^2 SCR$ under the same LOSO framework---is anticipated to confirm incremental performance gains from derivative information across all architectures, with the magnitude of improvement potentially varying by paradigm.

\subsection{Comparison with Prior Work}

Contextualising the present results against our prior evaluation of five deep architectures \cite{SanchezReolid2022}---which established PatchTST (F1 $=$ 0.852) as the best-performing model among 1D-CNN (0.796), TCN (0.812), InceptionTime (0.822), and TST (0.840)---allows us to address the central question motivating this work: can efficient and modern architectures approach or exceed PatchTST accuracy at substantially lower computational cost?

Three outcomes are possible, each with distinct implications. First, if one or more $O(L)$ architectures (Mamba, FEDformer, ModernTCN) achieve F1 within 1--2 percentage points of PatchTST, this would constitute strong evidence that the accuracy-efficiency trade-off can be substantially improved through paradigm innovation alone, without sacrificing predictive performance. Deployment-oriented research could then focus on these architectures with confidence that they do not represent a compromise on classification quality.

Second, if a significant accuracy gap persists between PatchTST and all other architectures ($\Delta \text{F1} > 3$ pp), this would indicate that patch-based tokenisation and self-attention provide a uniquely effective combination for EDA signals that current efficiency mechanisms cannot fully replicate. This outcome would motivate hybrid approaches---for instance, combining Mamba's selective state space for temporal processing with patch-based tokenisation to reduce effective sequence length---as a direction for future architectural innovation.

Third, if ModernTCN achieves performance comparable to the Transformer-based $O(L \log L)$ tier (Informer, Autoformer, TimesNet), this would validate the hypothesis that well-designed convolutions remain competitive for physiological time-series classification, consistent with ModernTCN's results on general time-series benchmarks \cite{ModernTCN2024}. Such a finding would support a pragmatic approach to architecture selection based on deployment constraints rather than a priori assumptions about paradigm superiority.

Regardless of which outcome materialises, the joint evaluation of accuracy and efficiency across eight architectures spanning five paradigms establishes an empirical foundation for evidence-based architecture selection in EDA-based affective computing---a foundation that, to our knowledge, has not previously existed in the literature.

% ======================================================
\section{Conclusion}

This study presented the first systematic comparison of efficient and modern architectures---spanning Transformers, state space models, and modernised convolutions---for subject-independent arousal classification from Electrodermal Activity. Eight architectures across five efficiency paradigms were evaluated under a strict Leave-One-Subject-Out validation framework using EDA signals from 147 participants: PatchTST (patch-based self-attention), Informer (ProbSparse attention), Autoformer (auto-correlation with decomposition), TimesNet (FFT-based 1D$\to$2D transformation), FEDformer (Fourier-enhanced attention), Mamba (selective state space model), ModernTCN (modernised depthwise convolutions), and DLinear (linear decomposition). By jointly evaluating classification performance and computational cost, this work provides the first explicit characterisation of the accuracy-efficiency trade-off for EDA classification across multiple architectural paradigms.

The inclusion of computational efficiency as a primary evaluation metric reflects a pragmatic recognition that the translational value of affective computing research depends not only on laboratory accuracy but on deployability in real-world, resource-constrained settings. The accuracy-efficiency Pareto frontier directly informs architecture selection under specific deployment constraints---wearable, mobile, or cloud---moving beyond the common practice of reporting accuracy in isolation.

The comparison between attention-based, state-space-based, and convolution-based paradigms further contributes to the broader methodological discussion on the necessity of architectural complexity for physiological time-series classification. The inclusion of DLinear and ModernTCN as critical baselines provides empirical evidence on whether complex temporal modelling mechanisms yield measurable benefits for EDA signals beyond what simple linear decomposition or well-designed convolutions can achieve.

Future work will explore three directions. First, self-supervised pre-training strategies tailored to the most promising architectures identified here \cite{MTECG2023} may improve performance on limited physiological datasets by leveraging unlabelled data. Second, multi-modal integration with complementary biosignals such as heart rate variability and electrocardiogram \cite{Oliver2025WESAD} may enrich the information available to lightweight classifiers without substantially increasing computational cost. Third, deployment-oriented optimisation through quantisation, pruning, and knowledge distillation \cite{Mukhopadhyay2024} applied to the Pareto-optimal architectures will further narrow the gap between laboratory performance and wearable deployment, enabling real-time, on-device arousal classification for mental health monitoring, stress detection, and adaptive human-computer interaction.

% ---- Bibliography ----
\bibliographystyle{splncs04}
\bibliography{bibliography.bib}
%
\end{document}
